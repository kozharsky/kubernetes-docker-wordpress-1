apiVersion: v1
kind: Service
metadata:
  name: blog1-wordpress
  labels:
    app: blog1-wordpress
spec:
  ports:
    - port: 80
  selector:
    app: blog1-wordpress
  clusterIP: None
---
apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: blog1-wordpress
  labels:
    app: blog1-wordpress
spec:
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: blog1-wordpress
        tier: frontend
    spec:
      containers:
      - image: kopachevsky/wordpress
        name: blog1-wordpress
        env:
        - name: WORDPRESS_DB_HOST
          value: "blog1-mysql"
        - name: WORDPRESS_DB_NAME
          value: "wordpress"
        - name: WORDPRESS_DB_USER
          value: "wordpress"
        - name: WORDPRESS_DB_PASSWORD
          value: "password"
        ports:
        - containerPort: 80
        volumeMounts:
        - name: blog1-wordpress-volume
          mountPath: /var/www/html/blog
      volumes:
      - name: blog1-wordpress-volume
        awsElasticBlockStore:
          volumeID: vol-fb8e7e44
          fsType: ext4