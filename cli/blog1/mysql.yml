apiVersion: v1
kind: Service
metadata:
  name: blog1-mysql
  labels:
    app: blog1-mysql
spec:
  ports:
    - port: 3306
  selector:
    app: blog1-mysql
  clusterIP: None
---
apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: blog1-mysql
  labels:
    app: blog1-mysql
spec:
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: blog1-mysql
    spec:
      containers:
      - image: mysql:5.6
        name: blog1-mysql
        env:
        - name: MYSQL_ROOT_PASSWORD
          value: "my-secret-pw"
        - name: MYSQL_DATABASE
          value: "wordpress"
        - name: MYSQL_USER
          value: "wordpress"
        - name: MYSQL_PASSWORD
          value: "password"
        ports:
        - containerPort: 3306
        volumeMounts:
        - name: blog1-mysql-volume
          mountPath: /var/lib/mysql
      volumes:
      - name: blog1-mysql-volume
        awsElasticBlockStore:
          volumeID: vol-3b8e7e84
          fsType: ext4