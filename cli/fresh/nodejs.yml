apiVersion: v1
kind: Service
metadata:
  name: fresh-nodejs
  labels:
    app: fresh-nodejs
spec:
  ports:
    - port: 8080
  selector:
    app: fresh-nodejs
  clusterIP: None
---
apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: fresh-nodejs
  labels:
    app: fresh-nodejs
spec:
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: fresh-nodejs
        tier: frontend
    spec:
      containers:
      - image: kopachevsky/ai-nodejs
        name: fresh-nodejs
        ports:
        - containerPort: 8080