apiVersion: v1
kind: Service
metadata:
  name: fresh-wordpress
  labels:
    app: fresh-wordpress
spec:
  ports:
    - port: 80
  selector:
    app: fresh-wordpress
  clusterIP: None
---
apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: fresh-wordpress
  labels:
    app: fresh-wordpress
spec:
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: fresh-wordpress
        tier: frontend
    spec:
      containers:
      - image: kopachevsky/wordpress
        name: fresh-wordpress
        env:
        - name: WORDPRESS_DB_HOST
          value: ai-mysql
        - name: WORDPRESS_DB_NAME
          value: "wordpress"
        - name: WORDPRESS_DB_USER
          value: "wordpress"
        - name: WORDPRESS_DB_PASSWORD
          value: "password"
        ports:
        - containerPort: 80
        volumeMounts:
        - name: fresh-wordpress-volume
          mountPath: /var/www/html/blog
      volumes:
      - name: fresh-wordpress-volume
        awsElasticBlockStore:
          volumeID: vol-7b39cfc4
          fsType: ext4