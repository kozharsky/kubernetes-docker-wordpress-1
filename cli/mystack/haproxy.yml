apiVersion: v1
kind: Service
metadata:
  name: mystack-haproxy
  labels:
    app: mystack-haproxy
spec:
  ports:
    - port: 80
  selector:
    app: mystack-haproxy
  type: LoadBalancer
---
apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: mystack-haproxy
  labels:
    app: mystack-haproxy
spec:
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: mystack-haproxy
    spec:
      containers:
      - image: kopachevsky/ai-haproxy
        name: mystack-haproxy
        ports:
        - containerPort: 80