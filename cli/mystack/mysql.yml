apiVersion: v1
kind: Service
metadata:
  name: mystack-mysql
  labels:
    app: mystack-mysql
spec:
  ports:
    - port: 3306
  selector:
    app: mystack-mysql
  clusterIP: None
---
apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: mystack-mysql
  labels:
    app: mystack-mysql
spec:
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: mystack-mysql
    spec:
      containers:
      - image: mysql:5.6
        name: mystack-mysql
        env:
        - name: MYSQL_ROOT_PASSWORD
          value: "my-secret-pw"
        - name: MYSQL_DATABASE
          value: "wordpress"
        - name: MYSQL_USER
          value: "wordpress"
        - name: MYSQL_PASSWORD
          value: "password"
        ports:
        - containerPort: 3306
        volumeMounts:
        - name: mystack-mysql-volume
          mountPath: /var/lib/mysql
      volumes:
      - name: mystack-mysql-volume
        awsElasticBlockStore:
          volumeID: vol-883acc37
          fsType: ext4