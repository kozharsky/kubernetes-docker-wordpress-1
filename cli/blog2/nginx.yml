apiVersion: v1
kind: Service
metadata:
  name: blog2-nginx
  labels:
    app: blog2-nginx
spec:
  ports:
    - port: 80
  selector:
    app: blog2-nginx
  clusterIP: None
---
apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: blog2-nginx
  labels:
    app: blog2-nginx
spec:
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: blog2-nginx
        tier: frontend
    spec:
      containers:
      - image: kopachevsky/ai-nginx
        name: blog2-nginx
        ports:
        - containerPort: 80