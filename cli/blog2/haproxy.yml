apiVersion: v1
kind: Service
metadata:
  name: blog2-haproxy
  labels:
    app: blog2-haproxy
spec:
  ports:
    - port: 80
  selector:
    app: blog2-haproxy
  type: LoadBalancer
---
apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: blog2-haproxy
  labels:
    app: blog2-haproxy
spec:
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: blog2-haproxy
    spec:
      containers:
      - image: kopachevsky/ai-haproxy
        name: blog2-haproxy
        ports:
        - containerPort: 80
        env:
        - name: WORDPRESS_ENDPOINT
          value: "blog2-wordpress"
        - name: NGINX_ENDPOINT
          value: "blog2-nginx"
        - name: NODEJS_ENDPOINT
          value: "blog2-nodejs"

