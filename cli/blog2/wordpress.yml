apiVersion: v1
kind: Service
metadata:
  name: blog2-wordpress
  labels:
    app: blog2-wordpress
spec:
  ports:
    - port: 80
  selector:
    app: blog2-wordpress
  clusterIP: None
---
apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: blog2-wordpress
  labels:
    app: blog2-wordpress
spec:
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: blog2-wordpress
        tier: frontend
    spec:
      containers:
      - image: kopachevsky/wordpress
        name: blog2-wordpress
        env:
        - name: WORDPRESS_DB_HOST
          value: "blog2-mysql"
        - name: WORDPRESS_DB_NAME
          value: "wordpress"
        - name: WORDPRESS_DB_USER
          value: "wordpress"
        - name: WORDPRESS_DB_PASSWORD
          value: "password"
        ports:
        - containerPort: 80
        volumeMounts:
        - name: blog2-wordpress-volume
          mountPath: /var/www/html/blog
      volumes:
      - name: blog2-wordpress-volume
        awsElasticBlockStore:
          volumeID: vol-cf887870
          fsType: ext4